{
  "name": "game-rps",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build",
    "build": "cat < `find src -name '*.css'` > build/styles.css",
    "lint": "yarn eslint src",
    "test": "yarn mocha -r esm"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^5.3.0",
    "eslint-config-airbnb": "^17.0.0",
    "eslint-plugin-import": "^2.13.0",
    "esm": "^3.0.84",
    "mocha": "^5.2.0"
  }
}
